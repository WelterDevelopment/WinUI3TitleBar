<Window
    x:Class="WinUI3TitleBar.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3TitleBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid x:Name="RootGrid"  >
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid Visibility="Visible" Grid.Row="0" Padding="6"  x:Name="CustomDragRegion"  IsHitTestVisible="False" Background="{ThemeResource WindowCaptionBackground}">
			<Grid.BackgroundTransition>
				<BrushTransition/>
			</Grid.BackgroundTransition>
			<TextBlock Text="{x:Bind WindowTitle}" VerticalAlignment="Center" Style="{ThemeResource BaseTextBlockStyle}"></TextBlock>
		</Grid>
		<Grid Grid.Row="1" x:Name="ContentGrid" Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"  >
			<Grid.BackgroundTransition>
				<BrushTransition/>
			</Grid.BackgroundTransition>
			<StackPanel>
				<ComboBox x:Name="ThemeBox" SelectedIndex="0" >
					<ComboBox.Items>
						<x:String>Dark</x:String>
						<x:String>Light</x:String>
					</ComboBox.Items>
				</ComboBox>
			</StackPanel>
		</Grid>
	</Grid>
</Window>
